# Fixes to handle

- while sending, display a status or open the log window and indicate when finished
- allow windows to minimize
- anchor does not work tag "anchor-data-id" should only be "id"
- when google ask for a token/credential renewal, send browser windows on top
- links cannot be edited
